# Roadmap

Living memory of the project. Single source of truth for scope, order and decisions.

---

## How to read this file

- **Phase** = epic. **Task** = one Pull Request. The checkbox tracks the task.
- Task IDs (`T-01`) are **stable and never reused**. They appear in the PR title.
- `[ ]` pending · `[x]` done
- No dates, no milestones, no deadlines. Relative order only.
- Every merged task updates this file in the same PR.
- Publishing content is not a task and carries no ID. See `project.md` §12.

---

## Definition of Done

A task is done only when all of the following hold:

- [ ] `lint` passes
- [ ] `typecheck` passes
- [ ] `build` succeeds
- [ ] No leftover `console.log`
- [ ] FSD import rules respected (no boundary violations)
- [ ] Both locales render, or the change is locale-independent
- [ ] `docs/roadmap.md` updated in the same commit

Squash merge is not on the list. It is the only strategy the repository allows, so it is
a repository setting rather than a condition the author can satisfy or verify.

---

## Open decisions

Decisions deliberately deferred. A task that depends on one of these **stops** until it
is resolved, and the resolution becomes a numbered entry in the Decisions Log.

| #    | Question                                                                               | Options on the table                                                                                                                                                                                          | Blocks                                                                          |
| ---- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| O-01 | How the landing sections are addressed                                                 | (a) single page, no per-section URL, nav scrolls · (b) hash fragments (`/#projects`) · (c) real routes per section                                                                                            | T-25                                                                            |
| O-02 | Localized pathnames (`/pt-BR/sobre` vs `/pt-BR/about`)                                 | Adopt `pathnames` in `defineRouting`, or keep one slug across locales                                                                                                                                         | Coupled to O-01 — only meaningful if sections become routes                     |
| O-03 | Motion library                                                                         | `motion`, `magicui`, or CSS-only                                                                                                                                                                              | T-34                                                                            |
| O-05 | How this site takes over `bernardomrl.dev`, and what becomes of the previous portfolio | (a) apex swap, previous site reachable only through its repository · (b) apex swap, previous site kept navigable at a subdomain — `v1.`, `old.` · (c) apex swap with the previous site's URLs redirected here | The cutover, and the removal of the `noindex` of D-73. Blocks no task before it |

O-01 was deferred on purpose. The route-per-section approach was designed in full and
rejected for now on maintenance grounds: it requires intercepting clicks, a `popstate`
listener to make the back button move the viewport, a pre-paint script to avoid a scroll
flash on cold entry, and a canonical tag on every duplicated route. None of that is
exotic, but all of it is bespoke behaviour that only the author can debug. Revisit once
the landing exists and the real navigation feel can be judged.

O-05 does not decide whether the previous portfolio appears in this one. It does: it is a
mature project and becomes a case study in `content/projects/`, which is ordinary T-21
scope. What is open is only whether it also stays navigable at its own address, and under
which name. `old.` is the obvious label and the weakest one — it tells a visitor the work
is obsolete before they have looked at it.

---

## Phase 0 — Foundation

- [x] **T-01** Initialize repository and Next.js 16 app with Bun; add `docs/` with `architecture.md` and `roadmap.md`; configure the `main` ruleset (deletions, force pushes, PR requirement)
- [x] **T-02** Configure `tsconfig` strict mode and the `@/*` path alias
- [x] **T-03** Configure Prettier and the ESLint flat config generated by the scaffold; add `.zed/settings.json` with format on save
- [x] **T-04** Configure `eslint-plugin-boundaries` with the FSD layer hierarchy, including the `src/app/**` and `src/proxy.ts` exceptions
- [x] **T-05** Set up Husky, lint-staged (`pre-commit`) and commitlint (`commit-msg`)
- [x] **T-06** Materialize the `shared` layer: move `globals.css` out of `app/` into `shared/ui/styles/` and repoint the import
- [x] **T-07** Add `browser-env.ts` and `.env.example` with Zod fail-fast validation
- [x] **T-08** Add `ci.yml` (lint, typecheck, build) and complete the `main` ruleset with the required status checks _(the `build` job must define `NEXT_PUBLIC_SITE_URL` in an `env:` block — D-54. It is public, not a secret. `lint` and `typecheck` never execute the module and need nothing)_
- [x] **T-09** Add `README.md`, `LICENSE` and `.github/PULL_REQUEST_TEMPLATE.md` _(local setup must document creating `.env.local` from `.env.example` as a prerequisite of `bun dev` — D-54)_
- [x] **T-10** Connect the Vercel project and point `next.bernardomrl.dev` at it _(`NEXT_PUBLIC_SITE_URL` must exist in the dashboard for Production and Preview before the first deploy — D-54. The apex and `www` stay with the previous portfolio — D-72)_

## Phase 1 — Design system

- [x] **T-11** Scope Tailwind's source detection to `src/` and add `cn()` in `shared/lib/` _(colour and radius tokens moved to T-12 — D-82)_
- [x] **T-12** Install Shadcn on Base UI: theme tokens from the preset, plus the first primitives the landing needs _(`init` rewrites the file it is pointed at, and the `source()` line of T-11 is the most likely casualty — run it with `--dry-run` and `--diff` first, and repeat T-11's phantom-utility grep as an acceptance criterion. `components.json` is written by hand beforehand, with `aliases.utils` targeting `@/shared/lib/cn.util` and the base pinned explicitly rather than left to the CLI default)_
- [x] **T-13** Typography foundation: apply the preset font and the base type defaults _(the `prose` style moves to T-19 — D-89. Not blocked by O-04: the theme provider is T-14)_
- [x] **T-14** Theme toggle: provider in `bootstrap/`, toggle as a feature slice _(creates `src/bootstrap/`, and with it `bootstrap/font.ts` — D-96)_

## Phase 2 — Internationalization

- [x] **T-15** `next-intl` setup: `routing.ts`, `request.ts`, the `withNextIntl` plugin, `src/proxy.ts` and the `[locale]` segment
- [x] **T-16** Message catalogs for `en` and `pt-BR`; locale switcher feature preserving the current pathname _(completes the README's "how to add a locale" section — D-68. Owns `messages` in `request.ts` and the `bootstrap/providers/` extraction deferred from T-15 — D-108, D-111)_
- [x] **T-17** Localized `generateMetadata`, `hreflang` alternates and canonical URLs _(first consumer of `browser-env.ts`; its Definition of Done includes proving that an absent or malformed `NEXT_PUBLIC_SITE_URL` aborts `bun run build` — D-56, shipping the site-wide `noindex` — D-73, and filling the repository's Website field on GitHub — D-77)_

## Phase 3 — Content layer

- [x] **T-18** Install and configure Velite: `velite.config.ts`, the `#site/content` alias, `.velite/` in `.gitignore`, `velite build` wired ahead of `typecheck` and `build` _(the alias must also be classified in the boundaries configuration — under D-43 an unresolved specifier is a local unknown, so `#site/content` errors until it is declared)_
- [x] **T-19** MDX pipeline: `rehype-pretty-code` with `shiki`, the MDX component mapping in `shared/ui/mdx/`, and the prose style inherited from T-13 _(D-89, D-90; `remark-gfm` is not installed — Velite applies it by default, D-141. `shadcn/typeset` was adopted and §10 of `architecture.md` renamed the class it names)_
- [ ] **T-20** `sections` collection and the `shared/content/` access seam _(completes the README's content authoring section, and ships `content/LICENSE` with the directory it creates — D-66, D-68; must prefix the `lint` script with `velite build` and amend §7 of `project.md` in the same pull request, or the first import of `#site/content` turns a required status check red on a clean checkout — D-140. `noUncheckedIndexedAccess` makes every indexed access to a collection possibly `undefined`, measured in T-19, so the seam exposes narrowing lookups rather than the raw arrays)_
- [ ] **T-21** `projects` collection and the `project` entity with its read helpers

## Phase 4 — Landing

- [ ] **T-22** Application shell: `site-header`, `site-footer`, root layout per locale _(creates `src/widgets/`; owns the localized `error.tsx` deferred from T-17 — D-128 — and the evaluation of `global-not-found.tsx` against the client-rendered 404 body of D-132)_
- [ ] **T-23** Hero section widget
- [ ] **T-24** About section widget
- [ ] **T-25** Projects section widget and section navigation _(blocked by O-01)_
- [ ] **T-26** Contact section widget: email with copy-to-clipboard, external links

## Phase 5 — Project pages

- [ ] **T-27** `/[locale]/projects/[slug]` with `generateStaticParams` across locales
- [ ] **T-28** Project detail layout: case study rendering, metadata, back navigation

## Phase 6 — Blog

- [ ] **T-29** `posts` collection, `post` entity, blog index with locale filtering
- [ ] **T-30** `/[locale]/blog/[slug]` with reading time and code block styling
- [ ] **T-31** RSS feed per locale

## Phase 7 — Portfolio ready

- [ ] **T-32** Dynamic Open Graph images with `next/og` for every document type
- [ ] **T-33** `sitemap.ts` and `robots.ts` derived from the content collections _(`robots.ts` allows crawling while the `noindex` of D-73 is in force — blocking it would defeat the directive)_
- [ ] **T-34** Motion pass across the landing, gated behind `prefers-reduced-motion` _(blocked by O-03)_
- [ ] **T-35** Accessibility audit: keyboard traversal, focus order, contrast in both themes, screen reader pass
- [ ] **T-36** Performance pass: Lighthouse, Core Web Vitals, bundle inspection, Vercel Analytics and Speed Insights
- [ ] **T-37** Content pass: write the real copy for every section in both locales, first case study, first post
- [ ] **T-38** Repository surface and security configuration: disable Issues, Projects, Wikis and Discussions as the execution of §12; enable branch auto-delete, auto-merge, secret scanning with push protection and Dependabot alerts; restrict default workflow permissions to read-only; and decide Dependabot version updates _(the version updates open pull requests carrying no task ID, which §12 of `project.md` forbids — resolving it amends §9 and §12 there, and the amendment belongs to this task)_

---

## Out of scope (MVP)

Any backend · database · authentication · contact form · payments · comments ·
newsletter · search · view counters · Sentry and external observability ·
automated tests · headless CMS

## Backlog (post-MVP candidates)

- GitHub API enrichment: stars, primary language, last push and topics on project cards, fetched at build time. Requires a token, therefore `server-env.ts`, and a `repository_dispatch` rebuild trigger to stay fresh
- Playwright smoke test covering both locales and one content route
- Search across posts, build-time index
- An AI feature (résumé chat, "ask about my projects"). This is the case that reverses D-19: markdown arriving token by token needs a runtime renderer, and `streamdown` is the right choice for it
- `perfectionist/sort-jsx-props`, and only with a custom group pinning `key`, `ref` and `className` ahead of the alphabetical run
- Third locale

---

## Decisions Log

Historical record. The current normative rule always lives in `architecture.md` or
`project.md`.

| #     | Decision                                                                                                                                                                                                                                          | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Rejected alternative                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-01  | `bootstrap/` replaces the FSD `app` layer and sits at the top; only `app/` imports it                                                                                                                                                             | Avoids the naming collision with the Next.js `app/` directory                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Keeping the layer named `app`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-02  | No `pages`/`views` layer; `app/` delegates straight to `widgets/`                                                                                                                                                                                 | The App Router already owns route composition; a views layer would duplicate it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Classic FSD with a `pages` layer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-03  | `entities/` and `features/` are configured from day zero but materialized on demand                                                                                                                                                               | A content site has few real domain objects and few real user actions; pre-created slices would hold what is substantially UI, turning the architecture into decoration and violating the rule of 3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Scaffolding all six layers as empty directories, mirroring the sibling project                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-04  | A single `@/*` alias mapped to `./src/*`; no per-layer alias, no `baseUrl`                                                                                                                                                                        | The layer stays visible in the import path, which is what a reviewer checks against the downward-only rule; `baseUrl` would let bare specifiers resolve from the source root and bypass the convention                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `@features/*`, `@entities/*` and siblings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D-05  | Boundaries enforced through `boundaries/dependencies`, not the `element-types` + `entry-point` pair                                                                                                                                               | Both were deprecated in v7; `dependencies` reproduces the entry-point behaviour through `element.fileInternalPath`, so the public-API rule and the layer rule live in one policy list instead of two that can drift apart                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Configuring `element-types` and `entry-point`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-06  | Strictness extended beyond `strict` with `noUncheckedIndexedAccess`, `verbatimModuleSyntax`, `erasableSyntaxOnly`, `noFallthroughCasesInSwitch` and `noImplicitOverride`                                                                          | Each closes a defect class the compiler can prove; `noUncheckedIndexedAccess` in particular guards content lookup by slug, the most likely runtime failure in this codebase                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `exactOptionalPropertyTypes` and `noPropertyAccessFromIndexSignature`, rejected as false-positive generators against React props                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-07  | `typecheck` runs `velite build && next typegen && tsc --noEmit`                                                                                                                                                                                   | Both Velite and the Next.js CLI emit uncommitted declarations; a bare `tsc` on a clean checkout type-checks against types that do not exist yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `tsc --noEmit` alone, which passes locally and fails in CI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-08  | `lint` runs with `--max-warnings 0`                                                                                                                                                                                                               | A rule at warning level exits zero and would pass a required status check, contradicting §2.3 rule 5 of `architecture.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Relying on reading CI output                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-09  | Prettier owns formatting; ESLint owns correctness and architecture. `eslint-config-prettier` sits last in the config array                                                                                                                        | The two tools overlap only on stylistic rules, and the overlap is what produces fights                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `eslint-plugin-prettier`, which Prettier's own documentation lists as not recommended                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-10  | Import order encodes the FSD layer hierarchy through `perfectionist/sort-imports` custom groups                                                                                                                                                   | The import block of any file becomes a map of the layers it depends on, so a reviewer sees a hierarchy violation before the linter reports it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Alphabetical-only ordering                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-11  | Conventional Commits without scopes, enforced mandatorily on the PR title                                                                                                                                                                         | Squash merge makes the PR title the surviving commit on `main`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Enforcing branch commits only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-12  | Roadmap task IDs stay in the `T-NN` form and are never expressed as `#NN`                                                                                                                                                                         | `#N` is GitHub's issue/PR namespace and its counter shifts with any discarded PR; a scope identifier cannot depend on infrastructure numbering                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Reusing PR numbers as task IDs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-13  | commitlint adopted with a `commit-msg` hook, validating every commit including the ones the squash discards                                                                                                                                       | A hook can only verify form, and a rule the hook contradicts in practice is a dead rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Relaxing the hook for branch commits                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-14  | Required approvals set to 0 in the branch ruleset                                                                                                                                                                                                 | A solo maintainer cannot approve their own pull request                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Requiring one approval                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-15  | `main` is created by an empty commit; every commit with content arrives through a pull request                                                                                                                                                    | A pull request cannot target a branch that does not exist, and an empty commit costs nothing while keeping the history free of exceptions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Pushing the scaffold directly to `main`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-16  | React Compiler not enabled                                                                                                                                                                                                                        | Stable in Next 16 but off by default; it depends on Babel and increases compile times, for a gain that is unmeasured on a codebase this small                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Enabling `--react-compiler` at scaffold time                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-17  | No `loading.tsx`; the page never awaits                                                                                                                                                                                                           | Route-level loading blocks navigation, and there is nothing asynchronous to wait for once content is compiled                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Route-level `loading.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D-18  | Velite is the content layer                                                                                                                                                                                                                       | Build-time validation with Zod, generated types, and — decisively — it runs as a standalone build step rather than a bundler plugin, so it is indifferent to Turbopack being mandatory here                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Content Collections, whose Turbopack support could not be confirmed; `@next/mdx`, which has no collections or typed frontmatter; a hand-rolled reader                                                                                                                                                                                                                                                                                                                                                                                              |
| D-19  | All markdown is compiled at build time; no runtime markdown renderer ships                                                                                                                                                                        | Both candidates parse in the browser, shipping a parser and the raw source for content that is fully known at build time, against stated criteria of performance and stability. `streamdown` was evaluated fairly and beats `react-markdown` on API, defaults, optional heavy plugins and documentation — it simply solves incomplete streaming input, a problem this site does not have                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `streamdown` and `react-markdown`. Reversed the day a runtime markdown source exists — see the AI feature in the backlog                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-20  | Syntax highlighting runs at build time through `shiki` and `rehype-pretty-code`                                                                                                                                                                   | Emits HTML with inline styles, so no highlighter reaches the client and there is no post-hydration colour flash; the VS Code grammars keep published code identical to the editor                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Prism or highlight.js, which colour after hydration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-21  | Case studies live in `content/projects/` inside this repository                                                                                                                                                                                   | Velite validates and types them, a malformed file fails the build, and adding a project is adding a file rather than touching code — which is the stated goal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | A `PORTFOLIO.md` in each project repository, which couples unrelated repos to this site; a separate content repository, which costs a token, a rate limit, a rebuild trigger and build-time validation, and only pays off with non-developer authors                                                                                                                                                                                                                                                                                               |
| D-22  | GitHub API integration demoted to a post-MVP enrichment                                                                                                                                                                                           | With case studies local, the API only adds live metadata. Making it foundational would drag a token, `server-env.ts` and a rebuild trigger into day zero for a decorative gain                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Fetching project data from the API as the primary content source                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-23  | No contact form; contact is an email address with copy-to-clipboard plus external links                                                                                                                                                           | The form skills it would demonstrate are already demonstrated in the sibling project's auth flow; here it would add spam handling, deliverability and a secret for no additional signal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | A Server Action with Resend; a third-party form service                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-24  | `defaultLocale: "en"`, `locales: ["en", "pt-BR"]`                                                                                                                                                                                                 | `defaultLocale` is the fallback for unidentified requests, and crawlers send no `accept-language`; Portuguese speakers are matched by header negotiation regardless                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `pt-BR` as default, which would serve Portuguese to every crawler and to every unmatched visitor                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-25  | `localePrefix: "always"`                                                                                                                                                                                                                          | Every document has exactly one canonical URL per locale, and no locale is privileged in the URL structure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `as-needed`, which leaves the default locale unprefixed and makes canonical and `hreflang` handling asymmetric                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-26  | A visible locale switcher is a requirement, not an enhancement                                                                                                                                                                                    | Header negotiation is a guess that fails on corporate machines, VPNs and personal preference; without a switcher the visitor is stranded                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Relying on automatic detection alone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-27  | Content is authored per file and per locale, with no requirement of a translated pair                                                                                                                                                             | Requiring translation doubles the friction of writing, which is the exact thing the blog exists to reduce                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Requiring every document in both locales; falling back to the other language silently                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-28  | Project documentation ships as `docs/architecture.md` and `docs/roadmap.md`; the delivery process rules are kept outside the repository                                                                                                           | Architecture and decision history are part of what a portfolio demonstrates; the Git and CI workflow is maintainer process and adds noise to a public repository                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Publishing all three documents at the repository root                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-29  | The global stylesheet lives in `shared/ui/styles/`, not in `app/`                                                                                                                                                                                 | `app/` is routing only; a global stylesheet carrying design tokens belongs to the design system                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Keeping `globals.css` where the scaffold puts it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-30  | `proxy.ts` hosts locale negotiation and nothing else                                                                                                                                                                                              | It sits at the network boundary; every additional responsibility there is one that runs on every request and is hard to test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Growing it into a general request-interception layer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-31  | No `server-env.ts` in the MVP                                                                                                                                                                                                                     | There is no secret to protect. Creating the module empty would be scaffolding for a case that does not exist                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Mirroring the sibling project's split env modules                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-32  | Every animation is gated behind `prefers-reduced-motion`, as a normative rule rather than a polish task                                                                                                                                           | Motion without the guard is an accessibility defect, and a portfolio that fails an accessibility audit fails at its own purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Adding the guard during the final pass                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-33  | Automated tests deferred past MVP                                                                                                                                                                                                                 | Delivery speed; a smoke test is queued in the backlog                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Testing from day zero                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-34  | `AGENTS.md` and `CLAUDE.md` generation disabled through `agentRules: false` in `next.config.ts`                                                                                                                                                   | `--no-agents-md` only covers the scaffold; from Next.js 16.3 `next dev` regenerates both files whenever it detects a coding agent in the environment, producing two files that are not part of the repository tree                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Git-ignoring them, which still writes them into the working tree on every `dev`                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-35  | `trustedDependencies` kept as generated; the `ignoreScripts` array removed from `package.json`                                                                                                                                                    | Bun's `trustedDependencies` replaces the default allow list rather than extending it, so dropping it would silence `sharp`'s postinstall; `ignoreScripts` is not a `package.json` field at all — the documented equivalent is the boolean `install.ignoreScripts` in `bunfig.toml`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Removing both as scaffold noise                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-36  | The `typecheck` script lands in T-02 in the partial form `next typegen && tsc --noEmit`; the `velite build` prefix is added by T-18                                                                                                               | Every task's Definition of Done requires `typecheck`, and T-02 is the task that defines `tsconfig` — deferring the script would leave the DoD unverifiable from T-02 to T-17. Velite does not exist yet, and a script invoking an absent binary fails                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Writing the full D-07 form in T-02, which would break until T-18; deferring the script to T-08 with the CI workflow                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-37  | Project knowledge syncs `docs/` from `main` through the GitHub integration; `src/` is deliberately excluded and `project.md` stays a manual upload                                                                                                | The sync is manual either way, so the gain is not automation but the removal of two failure modes: uploading the wrong revision and forgetting to upload at all. `src/` is excluded because a silently stale file is worse than an absent one — an absent file blocks explicitly, a stale one produces a confident wrong answer. `project.md` is not versioned (D-28)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Syncing the whole repository, which would make every source file a potentially stale snapshot; keeping the manual export of both documents                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-38  | Prettier is configured in `prettier.config.mjs` at the repository root                                                                                                                                                                            | The Tailwind plugin's options are plugin options, which Prettier does not validate — a typo in `tailwindStylesheet` fails silently, showing up as unsorted classes rather than an error. The `.mjs` form carries the `Config & PluginOptions` JSDoc type and a comment at the exact line T-06 will break; `package.json` is closed to extra fields                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `.prettierrc.json`, which loses both typing and comments and gains nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-39  | The `perfectionist/sort-imports` custom groups are configured in T-03, before the layers they match exist, and only `sort-imports` is enabled                                                                                                     | A custom group whose pattern matches nothing is inert, so the cost of configuring early is zero, while deferring would reopen `eslint.config.mjs` in a task that owns neither Prettier nor import order. The recommended preset is declined because it enables `sort-jsx-props`, deferred to the backlog                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Configuring the groups alongside `eslint-plugin-boundaries` in T-04; adopting `perfectionist.configs['recommended-natural']` wholesale                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-40  | Class sorting runs through `prettier-plugin-tailwindcss` with `tailwindStylesheet` pointing at the CSS entry point                                                                                                                                | Tailwind 4 has no config file, so the plugin resolves the theme from the stylesheet; without the option it sorts against an unconfigured default and the failure is invisible. The cost is a config file coupled to the stylesheet path, tracked in T-06                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Leaving class order to review; deferring the plugin to T-11 with the design tokens                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-41  | Element patterns anchored at the source root with `partialMatch: false` and an explicit `boundaries/root-path`                                                                                                                                    | The plugin matches patterns right-to-left by default, so `shared/*` would classify any nested folder named `shared` as the shared layer. The layout here is fully known, so anchoring costs nothing and removes a class of silent misclassification                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | The default partial matching, which only fails the day a nested folder happens to collide                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D-42  | `shared/` and `bootstrap/` are each a single element; only `widgets/`, `features/` and `entities/` are sliced                                                                                                                                     | `shared/` holds segments rather than slices, so cross-segment imports are internal to one element and are never evaluated. Slicing it would forbid `@/shared/ui` importing `@/shared/lib`, which §2.3 rule 4 of `architecture.md` declares legal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Slicing `src/shared/*`, which would require the barrel the architecture explicitly rejects                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-43  | `boundaries/flag-as-external` set to `{ unresolvableAlias: false }`                                                                                                                                                                               | An unresolvable `@/` specifier is flagged external by default and skipped without a message, which is exactly how this configuration fails silently. Treating it as a local unknown turns a broken alias into a permanent hard error. The cost is that `#site/content` must be declared in T-18                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Relying on the one-time fixture proof, which verifies the configuration on the day it lands and guarantees nothing afterwards                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-44  | `dependencies`, `no-unknown-files`, `no-unknown-dependencies` and `no-ignored-dependencies` enabled explicitly; neither predefined config used                                                                                                    | `recommended` disables the three unknown rules and `strict` enables the deprecated ones D-05 rejects. An explicit list is also the only form that stays reviewable against §2.3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `boundaries.configs.recommended`; `boundaries.configs.strict`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-45  | A terminal `disallow` policy exists solely to report public-API violations with their own message                                                                                                                                                 | Importing an internal segment otherwise matches no policy and falls through to the default, which reports it as a layer violation — false, since the layer is legal and only the entry point is not. The policy changes nothing about what is permitted, only how the refusal is explained, and the internal import is the most frequent violation in practice                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepting the generic message, which points the reader at the wrong rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-46  | `type-enum` restricted to the nine types of `project.md` §4.1; `subject-case` left at the preset default                                                                                                                                          | The preset ships eleven types, two of which §4.1 does not allow, while `git revert` keeps passing through commitlint's default ignores. The preset's `subject-case` already rejects `Fix: Some Message` and accepts `fix: handle empty frontmatter in defineCollection`; a stricter lower-case rule would turn every library, component or function name into a false positive, which is precisely the exception §4.1 carves out                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `subject-case: [2, 'always', ['lower-case']]`; keeping `revert` and `test` in the enum                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-47  | The pull request title is not validated by any hook, and the gap is accepted                                                                                                                                                                      | `commit-msg` is local and the squashed title is composed on GitHub, so no hook can reach it. Closing it would need a fourth CI job, and §7 of `project.md` declares exactly three. The convention stays enforced by the pull request template and by review                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Adding a title-lint job in this task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-48  | lint-staged and commitlint configured in `lint-staged.config.mjs` and `commitlint.config.mjs` at the repository root                                                                                                                              | `package.json` is closed to extra fields (D-38), and the project declares no `"type": "module"`, so a `.js` config would load as CommonJS and break `export default`. Both filenames are in each tool's documented lookup list, and `.mjs` matches the convention already set by `eslint.config.mjs` and `prettier.config.mjs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `lint-staged` and `commitlint` keys in `package.json`; `.lintstagedrc.json`, which loses comments                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-49  | lint-staged invokes the ESLint binary directly with `--max-warnings 0 --no-warn-ignored` instead of the `lint` script                                                                                                                             | The `lint` script lints the whole project, which defeats the point of a staged-file hook. ESLint 9 emits a warning for an explicitly passed file matching `globalIgnores`, and `--max-warnings 0` (D-08) turns that warning into a blocked commit; `--no-warn-ignored` exists for exactly this integration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Reusing `bun run lint` inside the hook                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-50  | The FSD skeleton is not scaffolded ahead of its content: a layer directory appears only when the task that writes its first file lands, so T-06 materializes `shared/` alone                                                                      | `project.md` §1 forbids committing an empty directory and rules out `.gitkeep`, so `bootstrap/` and `widgets/` cannot exist before T-14 and T-22 write into them. Same on-demand outcome as D-03, for a different reason — a repository rule rather than architectural intent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `.gitkeep` placeholders; keeping the original three-layer scope of T-06, two thirds of which the repository would refuse                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-51  | The global stylesheet is imported through the `@/` alias, exactly like a module                                                                                                                                                                   | §2.3 requires the layer to stay visible in the import path, and `boundaries/dependencies` classifies a CSS target the same way it classifies a module — a relative path would hide the layer and buy nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `../shared/ui/styles/globals.css` from `app/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-52  | The stylesheet import is left to fall into the `shared` import group rather than pinned at the top of the block                                                                                                                                   | With `side-effect-style` listed in `groups`, `sortSideEffects: false` stops ignoring the node, and a custom group outranks a predefined one, so `^@/shared/.+` claims it. There is one stylesheet and no CSS modules, so no cascade depends on its position; pinning it now would configure for a case that does not exist. Revisit the day a second stylesheet lands                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | An `elementNamePattern` excluding `\.css$` from the `shared` group; a dedicated custom group placed first in the list                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-53  | D-38's account of how a `tailwindStylesheet` mistake fails is corrected: a wrong path aborts the run, and a wrong option name is reported by Prettier but does not block it                                                                       | Measured in T-06 against a temporary `@utility`. A correct path sorts the custom utility after the built-ins; a nonexistent path throws `ENOENT` from `loadV4` and exits non-zero; a misspelled key produces `Ignored unknown option … Did you mean tailwindStylesheet?` on stderr, sorts against the default theme and exits zero. Prettier does validate plugin option names, since plugins register their own schema. D-38's decision stands — the `.mjs` form is still what carries the type and the comment — but the residual silent failure is narrower than recorded, and it is the only one the `lint` and `format:check` jobs of T-08 cannot catch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Rewriting D-38's rationale in place, which erases the premise the decision was actually taken on                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-54  | `NEXT_PUBLIC_SITE_URL` is required, with no default and no development-only fallback                                                                                                                                                              | A default lets a wrong host render successfully — canonical URLs, `hreflang` alternates, the sitemap and Open Graph images all point elsewhere and nothing fails. The cost is one line of configuration per environment, paid in T-08, T-09 and T-10                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | A fallback to `http://localhost:3000` outside production; a default always                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-55  | The validated object is built from literal `process.env.NEXT_PUBLIC_X` member expressions, one per key                                                                                                                                            | Next.js inlines public variables by static text replacement, and its documentation lists `process.env[name]` and reads through an intermediate variable as not inlined. `safeParse(process.env)` type-checks, builds, renders correctly on the server, and delivers `undefined` to the browser — the one failure mode with no loud symptom                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `browserEnvSchema.safeParse(process.env)`, which is shorter and silently wrong on the client                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-56  | T-07 ships `browser-env.ts` with no consumer; T-17 is the first consumer and owns the build-abort verification                                                                                                                                    | Both ways of manufacturing a consumer now are worse than none: prefixing `build` with a direct run introduces a second `.env` loader whose precedence differs from the one Next uses, and a side-effect import in `layout.tsx` is dead code. The fail-fast is proven in T-07 by exit code instead                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A `bun run` of the module prefixed to the `build` script; an unused import in `src/app/layout.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-57  | The `.env.example` placeholder is deliberately invalid rather than a working local value                                                                                                                                                          | The file documents the key; the README documents the value. A placeholder that works turns `cp .env.example .env.local` into a valid-but-wrong environment, and §9 requires the value to differ per environment, so a copyable value teaches the opposite model                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `http://localhost:3000`, which is correct in exactly one of the three environments                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-58  | The CI workflow declares three parallel jobs, one per required status check name                                                                                                                                                                  | A required status check is matched by check name, and §7 of `project.md` names three. One job with three steps reports a single check, and a matrix reports `ci (lint)` — neither name is requirable. No `needs:` between them: a skipped job never reports, and a required check that never reports is indistinguishable from pending                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | A single job with three steps; a matrix over the three commands; chaining `build` behind `lint` and `typecheck`                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-59  | Third-party GitHub Actions pass the same approval gate as packages, pinned to a full commit SHA with the version in a trailing comment                                                                                                            | `uses:` executes third-party code inside the runner alongside the checkout, which is the postinstall threat model rather than the import one. A tag is a mutable pointer, so a major-tag pin changes what runs with no diff in this repository. The cost is manual bumps, since the MVP has no Dependabot                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Pinning to a major tag; treating actions as infrastructure outside the dependency rule                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-60  | The runner installs Bun and Node; `next build` executes under Node, pinned literally to major 22                                                                                                                                                  | Bun respects the `#!/usr/bin/env node` shebang of the `next` binary and delegates to Node unless `--bun` is passed, so without a pin the runtime is whatever Node the runner image happens to ship. Vercel resolves the build runtime from `engines`, so an unpinned CI does not reproduce the deployment. T-10 must set the same major                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `setup-bun` alone; `node-version-file: package.json`, whose `>=20.9.0` range resolves to the highest available major and drifts; forcing `bun --bun run build`                                                                                                                                                                                                                                                                                                                                                                                     |
| D-61  | No dependency cache in CI                                                                                                                                                                                                                         | With four production dependencies, restoring and saving a cache entry costs about what the install costs. `setup-bun` already caches its own binary and `setup-node` v6 restricts automatic caching to npm, so neither needs an opt-out. Revisit when the install exceeds 30 seconds                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `actions/cache` keyed on `~/.bun/install/cache`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-62  | The `build` job sets `NEXT_PUBLIC_SITE_URL` to `https://example.invalid`                                                                                                                                                                          | `.invalid` is reserved by RFC 2606 and never resolves, so a CI artifact is unusable as a deployment by construction and the value teaches nothing copyable. It is not a secret: a `NEXT_PUBLIC_*` value is inlined into the client bundle. `lint` and `typecheck` receive nothing — neither ESLint nor `tsc` executes a module, and `next typegen` loads only `next.config.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | A repository secret; `http://localhost:3000`, which D-57 already rejected for `.env.example`; omitting the block until a consumer exists                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-63  | `permissions: contents: read`, `cancel-in-progress` only on pull requests, and no `paths` filter                                                                                                                                                  | Nothing in the workflow writes. Cancelling a superseded pull request run is free, while cancelling on `main` would leave the default branch without a check record. A `paths` filter would let a required check never report, which GitHub cannot distinguish from pending and which blocks the merge permanently                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Default token permissions; `cancel-in-progress: true` on every event; restricting the workflow to source paths to save runner minutes                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-64  | Stage 2 of the ruleset is configured after T-08 merges, not while its pull request is open                                                                                                                                                        | A check name is only selectable in the ruleset UI after the workflow has reported it at least once, and adding the requirement mid-pull-request risks blocking the task that creates the checks. Accepted cost: T-08's own pull request is the last merge not governed by status checks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Configuring stage 2 while the pull request is open; configuring it before the workflow ever ran, when the names do not yet exist                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-65  | D-53's reference to a `format:check` job in T-08 is corrected: T-08 ships three jobs, and formatting stays enforced by the `pre-commit` hook alone                                                                                                | §7 of `project.md` declares exactly three jobs and is normative, while the Decisions Log is a historical record by its own header — the reading D-47 already applied. A fourth job would not have caught the failure D-53 describes either: a misspelled option formats consistently against the default theme and `prettier --check` exits zero. The residual gap is real and accepted — formatting escapes through `--no-verify` or an edit made in the GitHub web UI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | A fourth job plus an amendment to §7 of `project.md`, which would edit a normative document to accommodate a job that does not close the gap it was proposed for                                                                                                                                                                                                                                                                                                                                                                                   |
| D-66  | `LICENSE` carries the verbatim MIT text and nothing else; the reservation over authored prose lives in the README, and `content/LICENSE` ships with T-20                                                                                          | GitHub's detection runs `licensee`, which matches the file against the canonical text — a scope note inside `LICENSE` costs the detected license type, which is a signal a portfolio repository is read for. The specific per-directory file is also the stronger instrument for narrowing MIT's "the Software", and until `content/` exists there is nothing to license                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | A single MIT covering the repository including the prose; a preamble above the MIT text carving out `content/`; no permissive license at all, which grants a reader of the code nothing                                                                                                                                                                                                                                                                                                                                                            |
| D-67  | The pull request template reproduces the Definition of Done as literal checkboxes, declared in an HTML comment as a projection of `docs/roadmap.md`                                                                                               | GitHub templates do not transclude, and §4.3 of `project.md` requires the checklist in the body. Naming the roadmap as the source inside the template is what keeps the copy from becoming a second normative list; a DoD change edits both in the same pull request                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Linking to `docs/roadmap.md` instead of listing the items, which produces an unusable checklist; omitting the checklist, which contradicts §4.3                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-68  | The README ships without the content authoring workflow §14 requires; T-16 adds the locale procedure and T-20 adds the frontmatter and file placement                                                                                             | Velite, `content/` and `next-intl` arrive in Phases 2 and 3, so the section could only be written as a description of a state that does not exist — and a README that describes the future is unverifiable by construction. Attaching the debt to the tasks that create the subject closes it at the only moment it can be written truthfully                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | A complete README written in the future tense; a placeholder section marked "coming soon"; a dedicated later task, which detaches the documentation from the feature that makes it true                                                                                                                                                                                                                                                                                                                                                            |
| D-69  | One CI status badge in the README, served by GitHub, unqualified by `branch`                                                                                                                                                                      | The differentiator of this repository is process discipline, and the badge is the only observable proof of it above the fold. The badge defaults to the default branch, so `?branch=main` is redundant. Accepted cost: an absolute `bernardomrl/portfolio` URL that renders the origin's status inside a fork, and a red badge whenever `main` is red — which is the point                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | A shields.io badge, which adds a third-party image host; badges for coverage or dependencies, which measure nothing this project has; no badge, which hides the one thing the reader should see first                                                                                                                                                                                                                                                                                                                                              |
| D-70  | The README documents `http://localhost:3000` as the local value of `NEXT_PUBLIC_SITE_URL`                                                                                                                                                         | Completes the split D-57 opened: `.env.example` documents the key with an invalid placeholder, the README documents the value. It is the port `bun dev` binds, so canonical URLs and Open Graph images resolve to the running server. Verified by exit code against `browser-env.ts`, not by reading the schema                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Repeating the value in `.env.example`, which D-57 rejected; leaving it undocumented, which makes `bun dev` fail on a clean clone with no instruction to recover                                                                                                                                                                                                                                                                                                                                                                                    |
| D-71  | The finished portfolio's repository was renamed to `portfolio.old` and this one from `dev` to `portfolio`                                                                                                                                         | Two repositories cannot share a name in one account, and the normative documents already named this one `portfolio` — §1 and §1.1 of `project.md` and the Autolink target in §4.2. Renaming before T-10 costs nothing; renaming after it would touch a live Vercel integration. GitHub redirects the previous paths, so no external link breaks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Keeping this repository as `dev` and amending three sections of `project.md` to match                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-72  | Production is `next.bernardomrl.dev`; the apex and `www` stay with the previous portfolio                                                                                                                                                         | The finished site is live and is not being retired to make room for one that has no content yet. `next.` names the relation between the two sites rather than the stage of either, so it survives the cutover — unlike `soon.` or `wip.`, which become false the day the site is finished, and unlike `dev.`, which reads as an environment this deployment is not                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | The apex from day one, which would take down a working site; `*.vercel.app` until the cutover, which wastes a domain already paid for                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-73  | Every route declares `robots: { index: false, follow: false }` until the apex cutover, and `robots.txt` keeps allowing crawling                                                                                                                   | Two sites on one domain compete for searches on the author's name, and the one with no content would dilute the one that has it. Indexed empty pages are also slow to undo. `robots.txt` is the wrong instrument: it governs fetching, not listing, so a blocked URL can still be listed — and a crawler denied the page never reads the directive that would exclude it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Disallowing the subdomain in `robots.txt`, which is the common mistake and produces the opposite result; indexing from day one and cleaning up later                                                                                                                                                                                                                                                                                                                                                                                               |
| D-74  | `engines.node` is pinned to `"22.x"` rather than declaring a floor                                                                                                                                                                                | Vercel documents `engines.node` as overriding the project's Node.js Version setting, and resolves an open range to the highest major it offers — `>=20.9.0` deployed under a major CI does not pin, which is the divergence D-60 exists to prevent, and the build log warns about the automatic upgrade on every run. The pin also makes a Node bump a reviewable edit in `package.json` and `ci.yml` instead of a silent change of runtime. Cost: §1.1 of `project.md` no longer describes a minimum                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Raising the CI pin to whatever Vercel currently resolves, which re-diverges on the next Node release; setting only the dashboard value, which `engines` overrides                                                                                                                                                                                                                                                                                                                                                                                  |
| D-75  | `NEXT_PUBLIC_SITE_URL` is scoped to Production and Preview with the same value                                                                                                                                                                    | Preview is a separate Vercel environment and D-54 forbids a default, so an unscoped variable is simply absent there and every preview build aborts from T-17 onward — removing the environment §6 of `project.md` defines as a complete isolated copy. The preview _hostname_ is automatic and unrelated: it is a URL Vercel assigns, not a value the application reads. Making it self-referential would mean deriving it from `VERCEL_BRANCH_URL` in `next.config.ts`, which §9 of `architecture.md` forbids and D-55 constrains                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Leaving Preview unset, which defers a guaranteed T-17 breakage; deriving the value in `next.config.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-76  | The `next` CNAME is unproxied in Cloudflare (DNS only)                                                                                                                                                                                            | Mirrors the apex, which is the host that actually serves content and resolves straight to Vercel's anycast addresses. The proxied `www` is not a counter-example: it only issues a redirect and never serves a document. Proxying would add a cache layer with invalidation independent of the deployment — one more variable in every diagnosis, for a site Vercel already serves from an edge                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | The proxied record, which is Cloudflare's default; mirroring `www`, which generalizes from a host that serves nothing                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-77  | The repository's Website field is filled in T-17, not in T-10                                                                                                                                                                                     | It is a public backlink from a heavily crawled page, and therefore the only discovery vector that makes the gap between the first deploy and the `noindex` of D-73 real — measured, since the production alias returns no `x-robots-tag` of its own. Nothing else links to the subdomain: no sitemap until T-33, no inbound reference. Moving the field to the task that ships the directive closes the window at zero cost                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Filling it in T-10 and accepting the exposure; anticipating an `X-Robots-Tag` header in `next.config.ts`, which creates a second normative mechanism for one rule                                                                                                                                                                                                                                                                                                                                                                                  |
| D-78  | This deployment is the Vercel project `portfolio-next`; the project named `portfolio` is the previous site, built from `bernardomrl/portfolio.old`                                                                                                | D-71 renamed the repositories and not the Vercel projects, so the project carrying the obvious name is the one that must not be touched. Renaming it would change the generated `*.vercel.app` aliases of a live site for a cosmetic gain. The project name is normative nowhere, and O-05 is the moment to revisit it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Renaming the previous project to free the name; creating this one in a second team                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-79  | The previous site's Vercel project received this repository's webhooks; the fault is recorded as an open item rather than worked around                                                                                                           | Vercel stores both the repository ID and its full name. The settings page resolves the ID and showed `portfolio.old` correctly, while webhook routing matched the stale name that D-71 freed and this repository then took — so a push here built in both projects. Disconnecting and reconnecting the previous project appears to have corrected it, but the fix is outside this repository and unverified beyond one push. The blast radius is bounded: the previous project's production branch is `production`, so a push to `main` can only produce a preview there, and the apex deployment was verified unchanged throughout. An Ignored Build Step would hide the fault from whoever configures the O-05 cutover                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Suppressing the builds with an Ignored Build Step; holding T-10 open, which does not stop the webhooks and blocks the roadmap on a fault that is not this task's                                                                                                                                                                                                                                                                                                                                                                                   |
| D-80  | Source detection is narrowed by `source()` on the `@import`, anchored at `src/`, rather than by `@source not` entries                                                                                                                             | The default base path is the repository root, so prose in `docs/` emitted `.table`, `.block`, `.static` and `.contents` into the bundle. An allowlist bounds the scan once: every prose directory that appears later — root markdown, `.github/`, a future `content/` — is outside by construction. A denylist needs an entry per offending path and fails silently, since the symptom is a phantom utility rather than an error. Cost: a class living outside `src/` needs an explicit `@source`, which is the correct thing to make visible                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `@source not "../../../../docs"`, which only fixes the directory that happened to be noticed                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-81  | `cn()` ships in T-11 as `src/shared/lib/cn.util.ts`, on `clsx` and `tailwind-merge`, with no `extendTailwindMerge`                                                                                                                                | `architecture.md` §10 makes `cn()` mandatory for every hand-written component, so it is the project's helper rather than Shadcn's. Letting the CLI create it would write `src/lib/utils.ts` — outside the layer hierarchy, without the §3 suffix, and rejected by `boundaries/no-unknown-files` — which turns T-12 into a rename. `tailwind-merge` 3.x covers Tailwind 4.0 to 4.3 and resolves the `--color-*` namespace without configuration, since colour groups accept any token after the prefix. Shipping without a consumer is the shape of D-56                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Deferring the helper to T-12 with the CLI; adding only `clsx`, which leaves conflicting `className` overrides unresolved in Shadcn primitives                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-82  | Colour and radius tokens belong to the Shadcn preset applied in T-12; T-11 ships none                                                                                                                                                             | Most of the site is Shadcn, and the components written by hand are styled on the same tokens, so the preset owns the contract. Hand-writing a minimal palette first would produce variables the CLI overwrites and a merge conflict against a generated block. Cost: no theme and no dark mode until T-12, and T-12 must not drop the `source()` line of D-80                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | A minimal six-token palette in T-11, sized by the rule of 3; the full palette written by hand ahead of the CLI                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-83  | O-06 closed: the previous project's stale Git binding is treated as repaired, on the absence of the symptom                                                                                                                                       | Two merges into `main` after the reconnect — T-11 and the one before it — produced no deployment in the previous project, where the fault produced one on every push. What is verified is the absence of the symptom and not the stored repository name: neither dashboard exposes that value, so direct verification is not on the table. Keeping the decision open after two consecutive negatives costs a revisit on every pull request, for an item D-79 already classified as cosmetic until the O-05 cutover — when both projects are reconfigured anyway, and when the stored name is read back by the act of reconfiguring them                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Holding O-06 open until the cutover, which pays a recurring cost for a fault with no remaining symptom; an Ignored Build Step, which D-79 already rejected for hiding the fault from whoever configures the cutover                                                                                                                                                                                                                                                                                                                                |
| D-84  | `components.json` is written by hand and `shadcn init` is never run; the CLI's work is reproduced by `bun add` and `shadcn apply`                                                                                                                 | `init` is terminal against an existing `components.json`: it offers to overwrite, and answering no aborts. There is no mode that reads an existing configuration — so the file this project needs cannot survive the command that was supposed to consume it, and `--force` would place `cn` at `src/lib/utils.ts` and detect a CSS path by heuristic before any repair is possible. Splitting it into `bun add` for the three packages and `apply --only theme` for the stylesheet is also the stricter reading of Rule A: every package enters by a named command instead of a CLI batch. The five aliases were verified by the `--dry-run --view` of `add button`, which resolved `cn` to `@/shared/lib/cn.util` and the file to `src/shared/ui/button.tsx`. Cost: `--pointer` was an `init` flag and its three lines are now hand-written in `@layer base`, and the `@layer base` block the CLI would have written is maintained here                                                                                                                                                                                                                                                                | Answering yes to the overwrite prompt and restoring `components.json` afterwards, which repairs the configuration after the wrong files are already on disk; moving the file aside, which has the same defect                                                                                                                                                                                                                                                                                                                                      |
| D-85  | The Vega style is adopted after Sera and Mira were measured and rejected; `neutral` base color kept against the recommendation of `stone`                                                                                                         | Sera hard-codes `rounded-none` and `uppercase` on the button, so the radius stops being a token and every future `add` arrives with a partisan the project would edit by hand. Mira's default button is 28px tall with `xs` and `icon-xs` at 20px, below the 24px floor of WCAG 2.2 SC 2.5.8 — a rule §11 of `architecture.md` already binds this project to. Vega derives every corner from `--radius`, which the preset set to `0.45rem`. The base color was chosen from the live preview, which outranks a description read from a changelog                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Sera, whose editorial framing does not survive its own button; Mira, whose compactness fails an accessibility rule the project audits in T-35; `stone`, rejected on sight                                                                                                                                                                                                                                                                                                                                                                          |
| D-86  | The dark mode selector contract is `@custom-variant dark (&:is(.dark *))`, written by the preset; O-04 is narrowed to persistence and class injection                                                                                             | The variant is emitted by `apply` and the `.dark` block carries every token override, so the selector half of the theming question is settled by the design system rather than by a provider choice. What remains open is who writes the class on the document, when, and how it survives a reload without a flash — which is the whole of what `next-themes` versus a hand-rolled provider decides                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Leaving O-04 stated in full, which would reopen a contract this task already fixed                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-87  | Exactly one primitive ships in T-12, and no barrel file is created in `shared/ui/`                                                                                                                                                                | `button` is the smallest artifact that exercises the whole chain — Base UI resolution, `cva`, `cn()`, the tokens, and the RSC boundary — and it proved the five aliases and the layer rules at once; installing more would be Rule C against a landing that does not exist until T-22. A barrel is legal here, since §2.3 rule 4 exempts `shared/`, but it would let a future `"use client"` leaf reach every consumer through one module, and it erases the segment-and-module detail the import path exists to show                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Installing card, input and separator ahead of the widgets that need them; an `index.ts` barrel re-exporting every primitive                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D-88  | The `source()` verification of D-80 is repeated with the natural grep and a temporary marker, without a positive control; the residual gap is accepted                                                                                            | The negative test only proves the boundary if the pipeline would emit the class when it sees one, and nothing in this task established that — the app has one scaffold page, so an empty stylesheet passes by omission rather than by exclusion. The positive control is one `sed` on the `@import` line, and it was abandoned mid-task after an unrelated `git checkout` destroyed the uncommitted stylesheet: a second deliberate mutation of the same file, minutes later, was not worth the exposure. What this task did verify is the question it owned — that `@import "shadcn/tailwind.css"` does not bring scanning of its own — and T-11 already proved the mechanism on the day the line landed. The gap is that the grep will keep passing if `source()` is ever removed and nothing else changes                                                                                                                                                                                                                                                                                                                                                                                             | Running the positive control anyway, which re-exposed a file that had just been lost; dropping the marker and keeping only the natural grep, which is the weaker of the two and the one that passes by omission                                                                                                                                                                                                                                                                                                                                    |
| D-89  | The prose style moves from T-13 to T-19; T-13 keeps the font application and the base type defaults                                                                                                                                               | Unlike D-56, D-81 and D-87, the contract is not knowable ahead of its consumer: half the rules style markup no build emits yet — shiki's inline-styled output under D-20 and the gfm tables — and the other half competes with the MDX component mapping, which decides per element whether it is styled by selector or replaced by a component. Shipping it in T-13 would produce CSS the Definition of Done cannot verify. What remains in T-13 is small and still homeless otherwise: the preset font has to land before the toggle of T-14 and the shell of T-22                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Shipping a partial prose style now for the elements whose markup is settled, which splits one definition across two tasks and still cannot be rendered; keeping T-13 as written and styling a DOM that does not exist                                                                                                                                                                                                                                                                                                                              |
| D-90  | The prose implementation is narrowed to `shadcn/typeset` versus `@tailwindcss/typography` and decided in T-19; no typography dependency enters in T-13                                                                                            | `@tailwindcss/typography` is a package plus a `@plugin` line and therefore a Rule A stop; `shadcn/typeset` has no npm package and no CLI command at all — it is one generated CSS file the project owns, with `:where()` guards and a `.not-typeset` opt-out, the same distribution model D-85 already adopted. Both satisfy §10 of `architecture.md`. Deciding now would fix a contract against markup D-89 just established does not exist                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Hand-written CSS with no guard system, which pays full maintenance for no gain over typeset; deciding for the plugin now and finding in T-19 that its modifier API is unused                                                                                                                                                                                                                                                                                                                                                                       |
| D-91  | Typographic tokens stay in `globals.css`; T-13 creates no second stylesheet                                                                                                                                                                       | D-40 points `tailwindStylesheet` at a single entry point, and the task's volume is one font family plus base defaults. A second sheet reached through a CSS `@import` from `globals.css` leaves D-52 untouched — that decision governs the position of a CSS side-effect import in the JavaScript import block, not an `@import` inside a stylesheet. The split is deferred to the file that will justify it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A dedicated `typography.css` in `shared/ui/styles/`, which reopens the entry-point question for two declarations                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-92  | Tailwind's default `--text-*` scale is kept; T-13 adds no size steps and no monospace family                                                                                                                                                      | Rule C. No widget exists to judge a bespoke scale against — the landing is T-22 and the first compiled document is T-20. A scale authored now would be measured against nothing and edited the first time real copy lands. `--font-mono` stays at the Tailwind default, since a monospace family is a code-block concern owned by T-19 and T-30. The `--font-heading` alias the CLI emitted from `fontHeading: inherit` was removed for the same reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | A bespoke seven-step scale sized for a landing that does not exist; keeping `--font-heading` as a no-op alias                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-93  | The positive control D-88 left open is executed in T-13, alongside the negative one, in a single build                                                                                                                                            | The condition that aborted it no longer holds: in T-12 the stylesheet was uncommitted and had just been destroyed by an unrelated `git checkout`. Here the marker is one utility class in `src/app/page.tsx`, the tree is committed before the build, and the reversion touches a file with no other pending edit. The emitted bundle carried the positive marker and not the negative one, so the boundary is now proven by exclusion rather than by omission. The grep pattern drops the leading zero, since the minifier emits `.1234em`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Repeating T-12's natural grep alone, which cannot distinguish exclusion from an empty pipeline; deferring the control to T-22, when the first real markup exists and the check stops being isolable                                                                                                                                                                                                                                                                                                                                                |
| D-94  | `shadcn apply --only font` deleted `--background` and `--foreground` from `:root`; the loss is repaired by hand and the command is classified as unsafe without diff review                                                                       | The CSS updater rewrote the head of the `:root` block and dropped the first two declarations. `@theme inline` maps both into `--color-*`, and `@layer base` applies `bg-background text-foreground` to `body`, so light mode lost its surface and its text colour while `.dark` stayed intact — a defect visible in one theme only. The stop point of the plan is what caught it, and it is the reason `apply` is never run without a clean tree and a full `git diff`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Trusting `--only font` as a scoped command; re-running `apply --only theme` to restore the block, which would revert the hand-written `@layer base` of D-84                                                                                                                                                                                                                                                                                                                                                                                        |
| D-95  | The font family is bound once, in `@layer base` on `html`; the `font-sans` class the CLI added to the JSX is removed                                                                                                                              | The CLI wrote the family twice — `@apply font-sans` in the stylesheet and `"font-sans"` inside `cn()` in the root layout. The CSS form survives anyone editing the layout and keeps a utility out of the JSX, which is what `app/` being routing-only means in practice. With one argument left, `cn()` became dead and its import went with it. The explicit rule is kept over relying on the preflight, which resolves through `--default-font-family`, a Tailwind-internal key                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Keeping the class in `cn()` and dropping the base rule, which puts the design system's font decision inside a route file; keeping both, which is two mechanisms for one rule                                                                                                                                                                                                                                                                                                                                                                       |
| D-96  | `bootstrap/font.ts` is deferred to T-14; T-13 calls `next/font` directly in the root layout                                                                                                                                                       | The destination is right — `bootstrap/` is one-off global configuration under §2.1, and rule 3 of §2.3 makes `app/` its only consumer, which is exactly who needs the font. The timing is not: D-50 gives the first file of the layer to the task that creates it, and the T-14 note already says so. With one family the extraction saves one line, and it pays for itself at three, which D-92 has just pushed past this task. The name carries no `.config.ts` suffix: the return of a build-time font loader is not a static configuration object, the exemption §3 already grants `browser-env.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Creating `src/bootstrap/` here, which makes the T-14 note false for an aesthetic gain; `bootstrap/font.config.ts`, whose suffix §3 reserves for static configuration                                                                                                                                                                                                                                                                                                                                                                               |
| D-97  | The `next/font` CSS variable is named `--font-sans-inter`; the theme's `--font-sans` composes it with an explicit fallback stack                                                                                                                  | `apply --only font` emitted `--font-sans: var(--font-sans)`, which is a CSS cycle. It resolves only because `next/font`'s declaration is unlayered and unlayered style outranks `@layer theme` regardless of specificity — `@theme inline` inlines a value into the utility and still emits the variable, contrary to what the comment first written here claimed. It also replaced Tailwind's fallback stack, so a missing `next/font` class would degrade to an invalid value rather than to a system font. Distinct names remove both the cycle and the dependency on cascade layer order                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Keeping the CLI's form and documenting the quirk, which leaves a working rule standing on an invisible mechanism; dropping the fallback stack, which makes the failure mode silent                                                                                                                                                                                                                                                                                                                                                                 |
| D-98  | The `shadcn` devDependency is pinned exactly to `4.16.2` rather than left at a caret range                                                                                                                                                        | It is a code generator, not a library: its output enters the repository as source, so a floating version produces different artifacts from the same command with no diff here to review — the reasoning of D-59 for actions and D-74 for the Node major. This task supplied the evidence, since `apply --only font` deleted two `:root` tokens (D-94). The mechanical point is narrower: every documented command already writes `shadcn@4.16.2`, so the caret governed only the local binary that `bunx shadcn` would silently resolve after any `bun install`. Cost: registry drift accumulates against a frozen CLI, and the bump is a deliberate edit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Removing the devDependency entirely, so no local binary exists and every invocation must name a version — defensible, and rejected only for leaving the project without a recorded intended version; keeping the caret, which lets the generator change with no diff                                                                                                                                                                                                                                                                               |
| D-99  | O-04 is closed on `next-themes`; a hand-rolled provider in `bootstrap/` is rejected                                                                                                                                                               | The pre-paint script, the live `matchMedia` tracking of `system`, `color-scheme` on the document and cross-tab sync are the whole of what the decision was about, and each is a defect class rather than a line count. Decisively, it is a layer argument: the toggle needs the hook, and an external package puts it outside the hierarchy, so `features/theme-toggle` imports `next-themes` and never `bootstrap/`, which §2.3 rule 3 forbids. A hand-rolled context would have to live in `shared/` with the provider in `bootstrap/`, splitting one unit across two layers to stay legal. Not pinned exactly: unlike `shadcn` (D-98) this is a runtime library, and its output never enters the repository as source                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | A hand-rolled provider, which reimplements four behaviours and pays the layer split                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-100 | §4 of `architecture.md` is amended: providers in `bootstrap/` are the single legal non-leaf client boundary, on the condition that they receive their subtree as `children`                                                                       | The leaf rule constrains the module graph, not the render tree. A Server Component that passes `children` as a prop renders that subtree on the server and hands the boundary an opaque node, so what ships is the provider's module and not the tree it wraps. A provider that imported its subtree would convert it, and the amendment leaves that a boundary error. §2.5 was the wrong instrument: its table is for files the framework forces outside the hierarchy, and `bootstrap/` is a layer §2.1 already defines                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Listing the provider in the §2.5 exceptions table; leaving §4 as written and accepting a rule the §8 requirement contradicts                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-101 | `features/` is materialized by `theme-toggle`, the first slice of the layer                                                                                                                                                                       | Not `widgets/`: a widget composes features and entities into a page block, and this composes nothing. Not `shared/`: that layer is the design system without domain knowledge, and which states the control exposes, in what order and under what label is application policy. It is an isolated user action, structurally the same object §2.4 uses `locale-switcher` as its example of — which is D-03's own test, not a waiver of it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `shared/ui/theme-toggle.tsx`, which puts application policy in the design system and defers the layer to T-16 for no gain                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D-102 | The toggle is mounted in `src/app/page.tsx` as a provisional host; T-22 removes it                                                                                                                                                                | `widgets/` does not exist until T-22, so `app/` is the only host available, and between its two files `page.tsx` is the scaffold placeholder T-22 and T-23 discard anyway. `layout.tsx` already changes for the provider and the font, and mounting UI there would give a routing file a second reason to exist. The debt is recorded here rather than in a comment alone, so the removal has an owner                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Mounting it in `layout.tsx`, which is the file §2.1 most wants thin; holding the toggle unmounted until T-22, which ships a feature nothing renders                                                                                                                                                                                                                                                                                                                                                                                                |
| D-103 | `@tabler/icons-react` is installed in T-14; no inline SVG and no `optimizePackageImports` entry                                                                                                                                                   | Counting glyphs is the wrong measure. `components.json` already declares `iconLibrary: tabler`, so the first CLI-generated component carrying an icon writes the import itself — the library arrives regardless, and inline SVG only guarantees the toggle will differ from everything installed after it, leaving a cleanup for a task that does not own it. §10 asks for visual coherence, and two icon sets is its opposite. The package is MIT, ships `"sideEffects": false`, and is on Next.js's default optimized-imports list, so an `experimental.optimizePackageImports` entry would configure a behaviour already on, inside a key the documentation marks as not recommended for production. Named imports are mandatory: `import * as` bundles the whole library                                                                                                                                                                                                                                                                                                                                                                                                                             | Two inline SVGs deferring the install to the first CLI icon, which pays the same install later plus a cleanup; adding the `experimental` entry, which is a no-op with a stability cost                                                                                                                                                                                                                                                                                                                                                             |
| D-104 | The control is two-state, over `defaultTheme: "system"` with `enableSystem`; the UI offers no way back to `system`, and the icons swap by CSS rather than by conditional render                                                                   | What `system` buys is the first paint honouring the operating system, and that is kept. Exposing it as a third state needs a menu primitive, which D-87 closed T-12 against and which no widget yet justifies. The two icons are swapped by the `dark` variant rather than by `resolvedTheme`: the hook returns `undefined` on the server, so a conditional render is hydration-unsafe by the library's own documentation, and the documented remedy — gating on `mounted` — removes the control from the first paint entirely and shifts the layout when it returns. The CSS swap resolves in the same paint as the `.dark` class the pre-paint script writes, which is what §11 requires of content while the page is still hydrating. Both icon modules ship either way, so nothing is saved by removing one from the tree                                                                                                                                                                                                                                                                                                                                                                            | A three-state menu, which installs a primitive for a landing that does not exist; gating the render on `mounted`, which blanks the control on first paint and trades an icon flash for a layout shift measured in T-36                                                                                                                                                                                                                                                                                                                             |
| D-105 | The `bootstrap/providers/` segment and a composing root provider are deferred to T-15; T-14 ships `theme-provider.tsx` flat                                                                                                                       | The destination is right and the timing is not — one provider composed by a root provider is an abstraction at the first case, the reasoning D-96 applied to `bootstrap/font.ts` one task ago. T-15 brings `NextIntlClientProvider` and makes the composition real, and the extraction is a file move plus one import. The shape is recorded now so it is not re-litigated: each provider takes `children` and nothing else, the composing module carries no directive because the boundary belongs to each provider file, and a component that renders sibling UI rather than context — a toaster — is wrapped the same way, to keep it out of `app/`, which §2.1 reserves for routing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Creating `providers/` and a root provider for a single provider; leaving the shape unrecorded and deciding it again in T-15 and T-26                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-106 | Archivo is the application typeface, and the `next/font` variable of D-97 is renamed to `--font-sans-archivo`; Abril Fatface is recorded as the display pairing and lands in T-22                                                                 | Inter was the scaffold's font and never a decision. Both faces are SIL OFL 1.1 and served from the `/google` loader, so no binary and no second license file enter a public MIT repository — which is what disqualified every other pairing considered: Tempting is a personal-use demo, Athelas, Vanguard and Balboa are commercial, and Arial and Times New Roman ship with an operating system rather than with a webfont license. Archivo carries 641 glyphs across 200 languages, so pt-BR needs no extra subset, and `next/font` generated the metric-matched `Archivo Fallback` face that the local route would have required by hand. The display half is only legal as Abril Fatface: the wider Abril family is TypeTogether's commercial product, and the OFL cut is single-weight 400. It stays out of T-14 because D-92 removed `--font-heading` one task ago on the grounds that no widget exists to judge type against, and a display face is that same bet. This entry supersedes the name in D-97; the mechanism that decision records is unchanged                                                                                                                                      | Switzer from Fontshare, which is legally clear and costs a versioned binary, a licence file, manual subsetting and hand-declared fallback metrics against the CLS budget of T-36; keeping Inter, which is the choice nobody notices                                                                                                                                                                                                                                                                                                                |
| D-107 | `request.ts` sits beside `routing.ts` in `src/shared/config/i18n/`, and the plugin is given the path explicitly; §6.1 of `architecture.md` amended to name both                                                                                   | The plugin's default lookup is `i18n/request.ts` at the root or under `src/`, and the layer hierarchy puts the directory neither place, so the default can never resolve it. The directory keeps the name `i18n` rather than `intl`: the location already diverges from every example in the library's documentation, and keeping the name is what leaves the divergence legible to a reader arriving from it — `intl` would also read as a wrapper around the ECMAScript `Intl` global, which the directory is not. Splitting the two files across directories was never on the table: `request.ts` validates against the contract `routing.ts` declares                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `shared/config/intl/`, which trades the last recognizable landmark for nothing; relying on the default lookup, which would require the file outside the hierarchy                                                                                                                                                                                                                                                                                                                                                                                  |
| D-108 | T-15 ships no `NextIntlClientProvider`; the `bootstrap/providers/` extraction of D-105 is triggered by T-16 instead                                                                                                                               | The provider exists only for client components consuming `next-intl` APIs, and T-15 has none — `theme-toggle` uses `useTheme`, Tabler and `Button`, and Server Components read from `request.ts` directly. Decisively, the shape is not knowable yet: what goes into `messages` is T-16's decision — whole catalog, per-namespace subset, or the `experimental.messages` loader — and two of the three produce a different provider. Shipping it here would reopen the file one task later to change the one prop that was its point. D-105's form is unchanged; only its trigger moves. Cost of deferring: one `git mv`, one new file and one import in T-16                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Creating `providers/` and the root provider around a single real provider plus one with no consumer, which is what D-96 and D-105 rejected twice in a row                                                                                                                                                                                                                                                                                                                                                                                          |
| D-109 | The root layout is `app/[locale]/layout.tsx`; `app/layout.tsx` is deleted rather than kept as a shell                                                                                                                                             | With every route inside the segment there is nothing above `[locale]` to lay out, and a second layout would exist only to render `children`. The font variable, `suppressHydrationWarning`, the `ThemeProvider` and the stylesheet import move down unchanged; `lang` stops being the literal `"en"` and reads the awaited param, and the signature follows the generated `LayoutProps<'/[locale]'>`. The scaffold has no `favicon.ico`, so nothing is left outside the segment. The global 404 is unaffected — `/_not-found` was prerendered as static in this build with no layout above the segment — so what remains for T-17 is the localized `not-found.tsx` inside it, not a missing shell                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A pass-through `app/layout.tsx` holding `html` and `body`, which `next-intl` cannot supply a locale to; keeping the metadata export above the segment, where T-17 could not localize it                                                                                                                                                                                                                                                                                                                                                            |
| D-110 | Static rendering comes from `next/root-params` plus `generateStaticParams` and `dynamicParams = false`; `setRequestLocale` is refused                                                                                                             | The library's own documentation marks `setRequestLocale` legacy and requires it in every layout and page that should render statically — a rule enforced by memory, and silently broken by the first file that forgets it. `next/root-params` makes the app eligible by construction and is available by default from Next.js 16.3, which is the version pinned here. `dynamicParams = false` closes the segment at build time: the locale set is fixed by `generateStaticParams` and no on-demand render path exists. It is not what produces the 404 for an unknown prefix, which was measured — with `localePrefix: "always"` the proxy reads `/xx` as a pathname carrying no locale and redirects to `/{negotiated}/xx`, so the segment never receives an unlisted value and the 404 comes from route matching. The `notFound()` in `request.ts` guards the same unreachable case and is kept as the module's contract. Verified in the build output — the route table, not the source                                                                                                                                                                                                               | `setRequestLocale` in every layout and page; a `hasLocale` guard plus `notFound()` in the layout, which duplicates a check `request.ts` already owns and guards a case the proxy has already normalized                                                                                                                                                                                                                                                                                                                                            |
| D-111 | `request.ts` returns `locale` alone in T-15; `messages` lands in T-16 with the catalogs                                                                                                                                                           | `messages` is optional in the return of `getRequestConfig` and only fails at the call site that asks for a message, and nothing in T-15 asks. Any placeholder catalog would be a file T-16 deletes, and `messages/` is a directory §1 of `project.md` forbids committing empty. The task closes with one line added in T-16                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | A stub `messages/en.json` to make the module look finished; an inline empty object, which types as a catalog and is not one                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D-112 | D-102's provisional host is now `src/app/[locale]/page.tsx`; T-22 remains its owner                                                                                                                                                               | The move is mechanical and the decision's substance is untouched — `widgets/` still does not exist, `app/` is still routing only, and the `site-header` of T-22 is still what removes the mount. The `why:` comment in the file names T-22 and §2.1 and never named a path, so it needed no edit. Recorded because the Log is read by path, and a stale one sends the next reader to a file that is gone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Editing D-102 in place, which erases the state the decision was taken in — the reading D-53, D-65 and D-83 already applied                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-113 | `request.ts` returns the whole catalog, loaded through an explicit map of static `import()` calls rather than a template literal                                                                                                                  | Per-namespace subsetting requires knowing which namespaces each route consumes, and there is one route; `experimental.messages` with `precompile` is experimental surface for a catalog of six keys. The map exists for a separate reason: a template literal inside `import()` is unresolvable for the compiler, so the module arrives as `any`, which Rule B forbids. Static specifiers keep each catalog typed and turn adding a locale into a reviewable edit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | The template-literal dynamic import, which is the library's documented form and silently untyped; namespace subsets, sized for a route table that does not exist; the experimental loader                                                                                                                                                                                                                                                                                                                                                          |
| D-114 | `NextIntlClientProvider` is rendered inside `root-provider.tsx`, with no props and no wrapper file of its own                                                                                                                                     | The library inherits `locale`, `messages`, `now`, `timeZone` and `formats` from `request.ts` when the provider is rendered from a Server Component, and `root-provider.tsx` is one — it carries no directive under D-105. A wrapper forwarding `children` and no props would be an abstraction at the first case, the reasoning D-96 and D-105 already applied twice. The order is intl outside, theme inside: inverting it puts the provider under a client component and forfeits the inheritance, forcing explicit props back in. Cost: the whole catalog crosses to the client, which is the library default and the correct trade at this size                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | A dedicated `intl-provider.tsx`; passing `messages` explicitly through `getMessages()`; the theme provider on the outside                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D-115 | `navigation.ts` joins `routing.ts` and `request.ts` in `src/shared/config/i18n/`, exporting `usePathname` and `useRouter` alone                                                                                                                   | `createNavigation` binds to the contract `routing.ts` declares, so it belongs beside it — the reasoning of D-107. Only the APIs with a consumer are exported: `Link`, `redirect` and `getPathname` arrive with the shell of T-22 and the metadata of T-17, and an export nothing consumes is surface that has to be kept working. §6.1 is amended to name the file, as T-15 did for `request.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Exporting the full return of `createNavigation`; a navigation helper local to `features/locale-switcher`, which hides the routing contract inside one consumer                                                                                                                                                                                                                                                                                                                                                                                     |
| D-116 | The switcher is a two-state button on the `button` primitive, and `features/locale-switcher` is the second slice of the layer                                                                                                                     | D-03 requires the layer argument even though §2.4 uses this exact name as its example. Not `widgets/`: it composes nothing. Not `shared/`: which locales exist, how they are labelled and the policy of preserving the pathname are application policy, and `shared/` is the design system without domain knowledge — the test D-101 applied to `theme-toggle`. With two locales there is exactly one alternative to the current state, so a menu would install a primitive D-87 closed T-12 against for a control that has nothing to enumerate. The third locale in the backlog is what reverses this, through a `shadcn add` under Rule A                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `select` or `dropdown-menu` now; `shared/ui/locale-switcher.tsx`, which puts application policy in the design system                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-117 | The pathname is preserved through `usePathname` and `router.replace`; query string and hash are dropped                                                                                                                                           | `usePathname` returns the path with the locale prefix stripped, which is exactly the argument `replace(pathname, { locale })` takes. `replace` over `push`: switching language corrects the current view rather than adding a step to the reading path, and a history entry per switch makes the back button toggle the language instead of leaving the page. Reading the query would require `useSearchParams`, which needs a Suspense boundary or opts the route into dynamic rendering — a decision entry under §4 — and no route carries a query or a fragment today. Option (b) of O-01 is what adds `window.location.hash` to the handler, safely, since it runs in the event                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `push`; reading `useSearchParams` for a case that does not exist; rebuilding the URL by hand from `window.location`                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-118 | The `aria-label` of `theme-toggle` moves into the catalogs in this task                                                                                                                                                                           | §5.3 classifies `aria-label` as a UI string belonging to `messages/`, and this is the first task in which the rule can be satisfied — T-14 shipped the slice with the label hard-coded in English because no catalog existed. Declared as a scope expansion under Rule E: one file, caused directly by the deliverable. It also makes `theme-toggle` the second consumer of the client provider, which is what proved D-114 end to end when removing the provider produced a missing-context error there                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Leaving the label until the shell of T-22, which keeps a known §5.3 violation in the tree for six tasks                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-119 | The React 19 warning about a script tag rendered by `next-themes` is accepted, with no code                                                                                                                                                       | Observed literally: `Encountered a script tag while rendering React component. Scripts inside React components are never executed when rendering on the client.`, with the stack resolving to `theme-provider.tsx`. Measured, not inferred: removing `NextIntlClientProvider` left the warning in place, so the composition of this task is not its cause, and the script is present in the served HTML and executes — `documentElement` appears once in the `curl` output. It is a false positive for the SSR pre-paint use case, open upstream against a library with no release since March 2025, and made loud by `browserToTerminal` in Next.js 16.2. Verified absent from a production build, so the cost is noise in `bun dev` and nothing in production                                                                                                                                                                                                                                                                                                                                                                                                                                          | `scriptProps={{ type: 'application/json' }}`, which stops the browser executing the script just measured working, and whose ternary returns different values on server and client — the classic hydration divergence; filtering `console.error` inside the provider, which swallows a class of warnings to hide one; replacing the library, which is Rule A and reverses D-99                                                                                                                                                                      |
| D-120 | `messages/` is declared as an element in the boundaries configuration, with one policy allowing `shared` to reach it                                                                                                                              | The catalogs resolve to local files, so under D-43 they are a local unknown and `no-unknown-dependencies` rejects every import of them — the failure D-43 chose to make loud, arriving one task before the `#site/content` case it named. §1 of `project.md` fixes the directory at the repository root, so relocating it under `src/` is a conflict rather than a fix, and `boundaries/ignore` would move the error to `no-ignored-dependencies` while hiding the dependency §2.5 wants visible. The plugin can only express the layer, so the confinement to `request.ts` is stated in §2.3 and in the module's JSDoc, the split §5.4 already uses for `#site/content`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `boundaries/ignore`, which trades one error for another and blinds the rule; moving the catalogs into `src/shared/config/i18n/`, which contradicts §1 of `project.md`; a `#messages/*` alias, which resolves to the same local file and changes nothing                                                                                                                                                                                                                                                                                            |
| D-121 | The theme flicker on locale change is repaired by re-applying the theme in a layout effect inside `ThemeProvider`; the root layout stays inside `[locale]`                                                                                        | Cause measured rather than guessed. Switching locale is a client-router navigation — every request carries `_rsc` and `text/x-component` and none is a document — so the pre-paint script and the full-document load first suspected of D-109 play no part. Changing the segment remounts the root layout, and React re-acquires the `html` singleton: it strips every attribute and rewrites only those coming from props, which is why a `data-probe` attribute nobody manages disappeared alongside the theme class and `color-scheme`. next-themes restores them in a passive effect, 3 ms later and after paint, which painted in one of six trials; its script cannot restore them, because React never executes a rendered script on the client — the same defect D-119 records. A layout effect runs after the mutation and before paint in the same commit, so the repair cannot be preempted by a frame: verified across six switches with every mutation landing on a single timestamp and no flicker. Residual gap: the reset itself remains, so any future imperative write to `html` is lost on a locale change                                                                            | Moving `html` and `body` into an `app/layout.tsx` above the segment, which is the fix that circulates for this combination: `lang` stops being derivable from a param that layout no longer sees, and decisively `[locale]` stops being a root param — the mechanism D-110 chose over `setRequestLocale` — so it reopens D-109 and D-110 for a 3 ms window; a document navigation in the switcher, which removes the whole class of defect and pays a reload on an action §6.3 makes mandatory and visible; gating on `mounted`, rejected by D-104 |
| D-122 | Metadata is split by scope rather than by file convenience: the segment layout declares `metadataBase`, `robots` and an inheritable `title`/`description`; every route declares its own title, description, alternates and full Open Graph object | Measured, not chosen: metadata merges shallowly between segments, so the `openGraph` envelope written in the layout vanished the moment `page.tsx` declared three fields of its own — `og:type`, `og:site_name` and `og:locale` were absent from the served `<head>` while `robots` survived only because no route redeclares it. What stays in the layout is what no route overrides. `title.template` is not adopted: with one route it has no consumer, and T-27 is the first. The `locale` param is narrowed by an `as` with a `// why:` under §13, since `LayoutProps`/`PageProps` type it as `string` and `dynamicParams = false` already closed the segment (D-110)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Declaring the envelope once in the layout, which is the form the App Router documentation's own example warns against; resolving the parent through `ResolvingMetadata` and spreading it, which pays an async parent resolution per route to reconstruct what repeating four literals gives for free; `title.template` now, sized for a route table that does not exist                                                                                                                                                                            |
| D-123 | The site title, description and `siteName` live in `messages/<locale>.json` under a `Metadata` namespace, read through `getTranslations` from `next-intl/server`                                                                                  | §5.3 divides by what the text is, not by where it is shown: these carry no paragraph, link, emphasis or structure, are not edited as writing, and removing them breaks the document rather than shortening it. They are the same family as the `aria-label` of D-118. `content/` does not exist until T-20 and would not change this — when the hero of T-23 lands, its headline is prose in `content/sections/`, a different text with a different owner, and the `Metadata` namespace does not follow it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `content/` as the home of the site description, which does not exist yet and is the wrong instrument regardless; hard-coding both in the layout, which is the §5.3 violation D-118 had just closed elsewhere                                                                                                                                                                                                                                                                                                                                       |
| D-124 | `alternateLinks: false` in `defineRouting`; the `hreflang` set of each route's metadata is the single normative source                                                                                                                            | Measured in T-16: the proxy answers every request with a `link` header carrying `rel="alternate"` for every locale of the contract. That set is unconditional, and §6.4 restricts alternates to the locales a document exists in — from T-27 onward the header is simply wrong. Emitting an indexing directive is also a second responsibility, which §6.2 denies the proxy. The flag lives in `defineRouting` because `next-intl` 3.22 moved middleware options there, so `proxy.ts` is not touched at all and keeps one responsibility and no configuration. Verified by the absence of the header on both locales                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Keeping both sources and reconciling them per document, which needs the route to correct the network layer on every future content route; `createMiddleware(routing, { alternateLinks: false })`, which the library has not accepted since 3.22 and which fails to type-check                                                                                                                                                                                                                                                                      |
| D-125 | `x-default` points at the document of `defaultLocale`, not at `/`                                                                                                                                                                                 | With `localePrefix: "always"` the root is a 307 and never a document, and an `hreflang` target that never answers 200 is the weaker form — the whole set stays self-canonical this way. Compatible with D-24 by construction: a crawler sends no `accept-language`, negotiation resolves it to `en`, and `x-default` names the same destination the redirect would have produced. The proxy's own header form did the opposite, pointing `x-default` at `/` on the grounds that the entry is reserved for language selection; the divergence is moot because D-124 removed the header                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `/` as the target, which is what the library's header emitted and what publishes a URL that is never a document; omitting `x-default`, which leaves an unmatched visitor to the crawler's guess                                                                                                                                                                                                                                                                                                                                                    |
| D-126 | The `noindex` of D-73 is declared once in the segment layout and inherited; §12 is amended to forbid any route redeclaring `robots` while it is in force                                                                                          | Every route lives inside `[locale]` (D-109), and a route that does not mention `robots` keeps the directive. Repeating it per route would produce N copies of a rule O-05 removes in one edit. The amendment is what makes inheritance safe: shallow merging means a route declaring `robots: { nocache: true }` replaces the object whole and drops `index` and `follow` with no visible removal — the same mechanism that ate the Open Graph envelope in D-122, caught there and forbidden here. Verified on `/en`, `/pt-BR` and the 404                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Repeating the directive in every route, which multiplies the edit O-05 has to undo; leaving §12's "every route declares" to be read literally, which the measured merge semantics turn into a trap rather than a safeguard                                                                                                                                                                                                                                                                                                                         |
| D-127 | `navigation.ts` adds `Link` and `getPathname` to its exports; `redirect` stays unexported                                                                                                                                                         | D-115 exports only the APIs with a consumer, and this task creates two: `getPathname` builds the canonical URL and every `hreflang` alternate, and `Link` is the locale-aware anchor of the localized 404. `redirect` still has none. Keeping the URL construction in `navigation.ts` rather than in the route is what leaves `routing.ts` the single place a prefix rule is expressed — a hand-built `/${locale}` string in metadata would silently outlive a change to `localePrefix`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Building the canonical and the alternates by string concatenation in `page.tsx`; exporting the full return of `createNavigation`, which D-115 already rejected                                                                                                                                                                                                                                                                                                                                                                                     |
| D-128 | The localized `error.tsx` §14 requires moves to T-22                                                                                                                                                                                              | It is a Client Component with a reset control and an error surface, which is site UI rather than metadata, and no shell, `widgets/` or design decision exists to render it against until T-22 — shipping it here produces a screen T-22 rewrites. `not-found.tsx` is not the same case: it is the direct consequence of the routing this task configures, and the metadata it inherits is this task's deliverable. Accepted cost: §14 is unsatisfied between T-17 and T-22, tracked on the T-22 line rather than left implicit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Shipping both here, which styles an error screen against a design system with no shell; leaving the debt uncorded and rediscovering it in T-35                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-129 | Open Graph ships as text only — `type`, `siteName`, `locale`, `alternateLocale`, `title`, `description`, `url`; `images` and an explicit `twitter` block wait for T-32                                                                            | The envelope is metadata this task already owns, and T-32 adds `images` to the same object additively. `og:locale` needs the underscore form of the protocol, which the routing locales do not give, so `og-locale.config.ts` holds the mapping as a static configuration object under §3 — supplying `US` for `en` is an editorial choice, not a derivation. Measured and left alone: the framework infers `twitter:card=summary`, `twitter:title` and `twitter:description` from `openGraph` with no block declared, so the Twitter tags ship regardless and T-32 owns changing the card when an image exists. The per-route repetition of the envelope is real; the extraction is justified at the third route, which is T-27 or T-29 (Rule C)                                                                                                                                                                                                                                                                                                                                                                                                                                                        | An explicit `twitter` block with `summary_large_image` and no image, which is the half-configuration T-32 would have to undo; deriving `en_US` from `en` by string transform, which invents a territory; extracting the envelope into a helper at the first consumer                                                                                                                                                                                                                                                                               |
| D-130 | The D-56 fail-fast is proven by exit code across four builds, with `.env.local` moved outside the working tree                                                                                                                                    | Reading the schema proves nothing about the build, and the module had never executed in a build before this task. Four cases, not two: absent, malformed, trailing slash — the `refine` that no consumer had ever exercised — and the `https://example.invalid` of D-62, which had to pass or this pull request would break the CI `build` job on merge. Results were 1, 1, 1, 0 with three distinct Zod messages, which is what proves the abort comes from `browser-env.ts` rather than from anything else in the build. The file goes to `/tmp` rather than to a suffixed copy, since `.gitignore` matches `.env*` and a suffix can escape it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | A `.env.local.bak` beside the original, which risks committing an environment file; testing only the absent case, which leaves the `refine` and the CI value unverified; asserting the behaviour from the schema                                                                                                                                                                                                                                                                                                                                   |
| D-131 | A catch-all `app/[locale]/[...rest]/page.tsx` calling `notFound()` is what makes the localized 404 reachable; it renders on demand and is the one route in this project that is not statically generated                                          | Measured: a pathname matching no route is a routing 404 and is answered by the global `/_not-found`, above the segment and outside every localization this task ships — the boundary of `[locale]` is only reached by an explicit `notFound()` raised inside it. §4 requires a decision entry for a page that cannot be statically generated, and this is it: the segment is closed by `dynamicParams = false` for the locale, while the catch-all necessarily accepts an unbounded set of pathnames and carries `dynamicParams = true`. The build table confirms the shape — `/en` and `/pt-BR` static, `/[locale]/[...rest]` dynamic. The status code is a real 404, because §14 forbids `loading.tsx` and the response therefore never streams. The boundary file sits in `[locale]/`, not beside the catch-all: `notFound()` resolves to the nearest ancestor boundary, so a sibling inside `[...rest]` serves that one call site and nothing else — the `notFound()` §7 requires of an unknown slug comes from `[locale]/projects/[slug]` in T-27 and `[locale]/blog/[slug]` in T-30, neither of which descends from it, and both would fall through to the unlocalized global 404 without noticing | Leaving the global 404 to serve unmatched paths, which is monolingual and unstyled; rendering the 404 UI directly in the catch-all without `notFound()`, which answers 200 and produces a soft 404; `not-found.tsx` beside the catch-all, which passes every test this task can run and silently stops covering the routes of T-27 and T-30                                                                                                                                                                                                        |     |
| D-132 | The body of the localized 404 is client-rendered and the gap is accepted; `global-not-found.tsx` is evaluated in T-22                                                                                                                             | Measured on a production server: the served HTML carries `<html id="__next_error__">`, an empty body and the whole not-found tree inside the flight payload, so the text exists in the response but not as DOM until hydration. Upstream and open — vercel/next.js#62228 — not a defect of this configuration. The status code and the metadata are correct, and the page is `noindex` regardless. `global-not-found.tsx` resolves at the routing level and is server-rendered, but lives outside `[locale]`, needs its own `html` and `body` and has no locale param to read, so adopting it now trades an empty body for a monolingual 404 against §6 — and the document it needs is exactly what T-22 builds                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `global-not-found.tsx` now, which costs the localization; rendering the UI in the catch-all to force server rendering, which D-131 already rejected for the 200; a client error boundary rethrowing to force a shell, which needs JavaScript for the same result                                                                                                                                                                                                                                                                                   |
| D-133 | `velite` enters as a `devDependency` at a caret range, not pinned exactly like `shadcn`                                                                                                                                                           | The application imports `.velite/`, which is generated JavaScript, and never imports the package, so nothing of it reaches the runtime graph. The exact pin of D-98 exists because the `shadcn` output enters the repository as source and drifts with no diff to review; this output is git-ignored and rebuilt on every run, and the version producing it is already frozen by `bun.lock` under `--frozen-lockfile` in CI and on Vercel. It is also 0.x, where a caret already restricts to patches inside one minor                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | An exact pin, which pays a manual bump for a version the lockfile already fixes; a runtime `dependency`, which describes a relation that does not exist                                                                                                                                                                                                                                                                                                                                                                                            |
| D-134 | T-18 ships `collections: {}` and does not create `content/`; the whole pipeline is proven with a fixture removed before the commit                                                                                                                | §1 of `project.md` forbids committing an empty directory and rules out `.gitkeep`, and every real collection belongs to T-20 or T-21. Measured rather than assumed: `velite build` exits 0 with no collections and with `content/` absent, and still emits `index.js` and `index.d.ts`, so the pipeline is verifiable without any content. The alias, the boundaries policy, the strict flag and the Turbopack resolution were all proven against a throwaway collection and two throwaway files — one valid, one missing a required field — reverted in the same step, which is the temporary-marker pattern of D-93, with `git status` as the proof that nothing generated was committed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | A minimal `sections` collection with one file, which pulls T-20 forward and produces content T-20 rewrites; deferring the alias and the classification to T-20, which leaves this task without the deliverable it exists for                                                                                                                                                                                                                                                                                                                       |
| D-135 | `root` and `output.data` are declared explicitly at their default values; `output.clean` is deliberately absent                                                                                                                                   | Both declared values are normative elsewhere — §1 of `project.md` fixes `content/` at the repository root and §2.3 of `architecture.md` maps `#site/content` to `.velite` — so a library default shifting in a minor would move the content root or the alias target with no edit here. `clean` is not declared because it cannot work from this file: it is one of the two options D-139 measured to be unreachable when the CLI is the caller, and a config key that states an intent it never has is worse than its absence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Relying on the defaults for `root` and `output.data`, which leaves two normative paths implicit; declaring `output.clean: true`, which reads as configuration and is dead code; touching `output.assets`, which nothing writes to until content exists — T-20 owns whether it is git-ignored                                                                                                                                                                                                                                                       |
| D-136 | `#site/content` is classified as the boundaries element `content`, pattern `.velite`, and the plugin's file-pattern warning is an accepted cost                                                                                                   | The generated entry resolves to a local file, so under D-43 it is a local unknown and every import errors until declared — the case D-43 named, arriving as predicted. Element rather than external is not a choice: `flag-as-external` only reaches specifiers that do not resolve. The name matches the `content` custom group already configured in `perfectionist/sort-imports`. The plugin prints a console warning because its heuristic reads the dot in `.velite` as a file extension; two attempts to silence it were measured and both broke classification, since `partialMatch: false` makes the pattern a folder pattern and makes `mode` inert — the plugin says so itself. The warning is not an ESLint warning, so `--max-warnings 0` never sees it, and the classification is proven both ways: `shared → content` passes and `feature → content` errors by policy rather than by being unknown                                                                                                                                                                                                                                                                                         | `pattern: '.velite/*'`, which the plugin rewrites to `.velite/*/**/*` in folder mode and which desclassified the target; adding `mode: 'file'`, deprecated in v7 and explicitly ignored alongside `partialMatch: false`; `boundaries/ignore`, which trades one error for another and blinds the rule (D-120)                                                                                                                                                                                                                                       |
| D-137 | `allowImportingTsExtensions` is refused; the generated `index.d.ts` importing `'../velite.config.ts'` with its extension is measured to be harmless                                                                                               | Velite writes the config path with its extension into the emitted declaration, the form TS5097 rejects under `moduleResolution: "bundler"`. Measured instead of inferred, and the first measurement was a false negative: `.velite/` is a dot-directory, so the `include` globs skip it and a bare `tsc --noEmit` never loaded the file. Forcing it into the program with a probe import gave exit 0, and repeating with `--skipLibCheck false` produced 21 unrelated baseline errors and none in that file — so the flag is not masking it either. The restriction guards emitted output, and a declaration file emits nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Adding the flag on the strength of reading the generated file, which configures the project for an error that does not occur; concluding from the first `tsc` pass, which was a file the compiler never opened                                                                                                                                                                                                                                                                                                                                     |
| D-138 | `velite build` does not run in watch mode; `dev`, `build` and `typecheck` are each prefixed with a one-shot run, in `package.json` rather than in `ci.yml`                                                                                        | A parallel watcher needs a process runner, which is Rule A for a convenience with nothing to watch until T-20. The plugin integration the library documents for Next.js is a webpack plugin, and D-18 chose Velite precisely for being a standalone step indifferent to Turbopack — adopting it would reverse the argument of the choice. The prefix lives in `package.json` because Vercel runs that `build` script: with the prefix in CI only, CI stays green while production builds without `.velite/` and the alias fails to resolve. `ci.yml` needs no edit, since its three jobs already invoke the scripts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `velite --watch` behind `concurrently` or `npm-run-all`; the webpack plugin, which contradicts D-18; a separate `velite build` step in `ci.yml`, green in CI and broken on Vercel                                                                                                                                                                                                                                                                                                                                                                  |
| D-139 | `--clean` and `--strict` are passed as CLI flags in the scripts; neither is declared in `velite.config.ts`, because the config value is unreachable when the CLI is the caller                                                                    | §5.4 of `architecture.md` requires malformed frontmatter to fail the build, and Velite's default does the opposite: it reports the issue and exits 0, which was measured here when a probe missing a required field let `next build` run through the `&&`. Setting `strict: true` in the config changed nothing, and the mechanism is in the distributed source: the CLI parser declares every flag with `default: false`, and `resolveConfig` merges as `options.strict ?? loadedConfig.strict ?? false` — `??` only falls through on `null` or `undefined`, so the config is never consulted. `output.clean` has the identical shape one line above. `--strict` on the command line was verified to abort with `Schema validation failed.` and exit 1, and `--clean` to leave the freshly written output intact. The asymmetry is worth remembering: `root`, `output.data`, `output.assets`, `base`, `name` and `format` read the config directly and work — only the options that are also CLI flags are affected                                                                                                                                                                                     | Declaring both in the config, which reads as configuration and has no effect; declaring both in the config _and_ on the command line, two mechanisms for one rule (D-95); amending §5.4 to describe what the tool does by default, which would trade a normative rule for a log line                                                                                                                                                                                                                                                               |
| D-140 | The `lint` script is left unprefixed in T-18; the debt is recorded on the T-20 line                                                                                                                                                               | Nothing imports `#site/content` yet, so the job is green as it stands and prefixing now would configure for a case that does not exist. The break is real and dated: the `lint` job runs against a clean checkout with no `.velite/`, and D-43 makes an unresolved specifier a hard error, so T-20's first import turns a required status check red. Attaching it to the task that creates the import is what makes it impossible to miss, together with the §7 amendment it needs. Residual gap: the `pre-commit` hook invokes ESLint directly (D-49) and fails the same way on a clone where no build has run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Prefixing the script and amending §7 here, for an import six files away; leaving the consequence undocumented and rediscovering it as a red check in T-20                                                                                                                                                                                                                                                                                                                                                                                          |
| D-141 | `remark-gfm` is not installed; Velite's own `gfm` default is the mechanism                                                                                                                                                                        | The T-19 line named the package, and the distributed source contradicts it: `MdxOptions.gfm` defaults to `true` and pushes `remarkGfm` into the plugin list before any configured plugin runs. Declaring it in `mdx.remarkPlugins` applies the same transform twice — two mechanisms for one rule, which D-95 and D-139 both rejected. Verified on the probe output: tables, strikethrough, task lists and autolink literals all compiled with the package absent from `package.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Installing it and declaring it, which is redundant by measurement; installing it and not declaring it, which is a dependency with no importer                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-142 | Every collection compiles through `s.mdx()`; `s.markdown()` is not used                                                                                                                                                                           | `s.markdown()` emits an HTML string, which has no component mapping — and §10 requires content images to resolve through one, while §6 requires prose links to carry the locale prefix. Regra F is satisfied because the markdown never survives the build: parsing, GFM and the rehype pass all run inside `velite build`, `.velite/` stores JavaScript, and `MdxContent` is a Server Component on a statically generated page, so `new Function` executes once at build time. Measured: the emitted `code` opens with `arguments[0]` and returns a module object, `/en` and `/pt-BR` build as SSG, and no `micromark` or `mdast-util` appears in the client chunks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `s.markdown()` with `dangerouslySetInnerHTML`, which forfeits the mapping §10 mandates; bundling MDX through the esbuild recipe, which pays Rule A for three packages to solve a redundancy this project does not have                                                                                                                                                                                                                                                                                                                             |
| D-143 | Only the `mdx` block is declared in `velite.config.ts`; there is no `markdown` block                                                                                                                                                              | With D-142 there is no `s.markdown()` call to configure. A block of remark and rehype options attached to a schema nothing invokes reads as configuration and is dead — the same fault D-134 named for `output.clean` and D-139 for `strict`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Declaring both blocks for symmetry, which configures a path with no caller; sharing one options object across both, which hides the same dead branch behind a constant                                                                                                                                                                                                                                                                                                                                                                             |
| D-144 | D-90 is resolved for `shadcn/typeset`; §10 of `architecture.md` renames the class from `prose` to `typeset`                                                                                                                                       | Three grounds, none of preference. It introduces no package and no `@plugin` line, so Rule A is untouched. Its `:where()` guards carry zero specificity, which is the only form that survives a component mapping — with `@tailwindcss/typography`, `.prose a` outranks the utilities of the mapped anchor and forces `not-prose` and `prose-a:` escapes. And the plugin styles `pre` and `code` exactly where `rehype-pretty-code` already rules, so adopting it costs a row of `prose-pre:` declarations undoing what it just applied. The CLI risk of T-13 does not apply: typeset has no CLI command, so `components.json` is never reopened                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `@tailwindcss/typography`, which is a package plus a plugin line and fights the highlighter; hand-written CSS with no guard system, which D-90 had already rejected                                                                                                                                                                                                                                                                                                                                                                                |
| D-145 | The sheet is authored against this project's tokens rather than copied from the registry, and enters `globals.css` through `@import './typeset.css' layer(components)`                                                                            | The registry file knows nothing of `--shiki-light`, of the `.dark` class of D-86 or of the `data-rehype-pretty-code-*` attributes, so it would be rewritten on arrival and the diff would be unreviewable. The layer is not cosmetic: an unlayered rule outranks every `@layer` regardless of specificity — the mechanism D-97 measured with `next/font` — so an unlayered typeset would beat the utilities of any component mapped into the prose, which is the property the `:where()` guards exist to guarantee. This is the second stylesheet D-91 deferred to the file that would justify it. Measured: `globals.css` grew by five lines, the import and its comment, and the production CSS carries `.typeset` and the plugin's attribute selectors                                                                                                                                                                                                                                                                                                                                                                                                                                                | Pasting the registry file and patching it, which produces a vendor diff nobody can review; leaving the import unlayered, which reverses the guarantee of the guards                                                                                                                                                                                                                                                                                                                                                                                |
| D-146 | `shared/ui/mdx/` is created in T-19 with no consumer in the merged tree, and `MdxContent` applies the `.typeset` wrapper itself                                                                                                                   | The renderer is the only instrument that can verify what this task ships: the prose style and the highlighter output are both invisible without a rendered DOM, and being unverifiable is precisely why D-89 moved the prose here. Regra C is not engaged — nothing is abstracted, the component map is a module constant with one entry. The compiled output confirms the mechanism: the element map spreads `...n.components` last, so a mapped element replaces the default rather than being ignored. The wrapper lives in the component because §10 requires the style to be defined once, and a class the consumer must remember is defined once only by convention                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Deferring the whole slice to T-20, which returns the prose style to the unverifiable state D-89 diagnosed; leaving the class to the consumer, which makes §10 a convention rather than a rule                                                                                                                                                                                                                                                                                                                                                      |
| D-147 | Only `a` is mapped; `img` is deferred to the task that ships the first content image                                                                                                                                                              | The anchor is mapped for correctness and not for style: with `localePrefix: 'always'` and `dynamicParams = false`, a raw `/about` in prose resolves to a path that does not exist. Measured on the probe: the internal link rendered as `/en`. `img` is the opposite case — §10 mandates `next/image`, which needs intrinsic dimensions, and the markdown body carries none; only `s.image()` in a schema emits them, and no schema exists until T-20                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Mapping `img` to `next/image` with `fill`, which fixes a layout contract against markup that does not exist; mapping it to a plain `<img>`, which violates §10 on the day it lands                                                                                                                                                                                                                                                                                                                                                                 |
| D-148 | `theme` takes two entries and `keepBackground` is `false`; the dark branch selects on the `.dark` class rather than on `prefers-color-scheme`                                                                                                     | A single theme writes fixed colours into markup at build time, and the site has two that a reader toggles long after Velite ran. `defaultColor` is not an option of this plugin — the published `Options` interface does not carry it — so the documented pattern with `--shiki-light` and `--shiki-dark` is the mechanism. It works without `!important` only if the plugin emits no inline `color`, which was verified on the probe output: `style:{"--shiki-light":"#D73A49","--shiki-dark":"#F47067"}`, variables and nothing else, with `data-theme` carrying both names space-separated as the selector requires. The documented rule branches on the media query, which would ignore an explicit choice and contradict D-86; the class form was confirmed in the browser, tokens changing on toggle without a reload. The `mark` of highlighted characters was left unstyled after reading it legible in both themes                                                                                                                                                                                                                                                                              | A single theme, which is wrong in one of the two modes by construction; the media-query form the plugin documents, which overrides the reader's explicit selection; `keepBackground: true`, which puts a foreign surface colour next to `--card`                                                                                                                                                                                                                                                                                                   |
| D-149 | `copyLinkedFiles: false` in the `mdx` block; `output.assets` is never written                                                                                                                                                                     | The plugin resolves every link href against the content root and reads it from disk, to copy the target into `output.assets`. This project has no such target — §10 references content images from `public/` and resolves them through the component mapping — so the option only turns navigation hrefs into file reads. Measured, and the mechanism read back from the installed source: `join(absoluteRoot, value)` collapses `[link](/)` onto the content root                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
