# Public origin of the deployment being viewed. Feeds canonical URLs, hreflang
# alternates, the sitemap and Open Graph images, so it MUST differ per
# environment and MUST NOT end with a trailing slash.
#
# This placeholder is deliberately invalid: copying this file to .env.local
# without editing it has to fail, not silently produce a working-but-wrong
# environment. Local development uses http://localhost:3000
NEXT_PUBLIC_SITE_URL=replace-me
